{% extends 'base.html.twig' %}

{% trans_default_domain 'participants' %}

{% block content %}
    <table class="table">
        <caption>{{ 'table.description'|trans }}</caption>
        <thead>
        <tr>
            <th>{{ 'table.position'|trans }}</th>
            <th>{{ 'table.username'|trans }}</th>
            <th>{{ 'table.email'|trans }}</th>
            <th>{{ 'table.participation_number'|trans }}</th>
        </tr>
        </thead>
        <tbody>
        {% for participant in participants %}
            <tr {% if loop.first %}class="success"{% endif %}>
                <th scope="row">{{ participant.position }}</th>
                <td>{{ participant.username }}</td>
                <td>{{ participant.email }}</td>
                <td>{{ participant.participationList|length }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}